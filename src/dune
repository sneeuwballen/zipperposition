(env
  (_ (flags :standard -warn-error -3 -w -3 -color always -safe-string))
  (_ (ocamlopt_flags :standard -no-assert -O3 -unbox-closures -unbox-closures-factor 20))
)

(alias
 (name    runtest)
 (package zipperposition)
 (deps    %{bin:zipperposition.exe} Makefile (source_tree ../examples/))
 (action  (run ./test_must_pass.sh %{bin:zipperposition.exe})))

