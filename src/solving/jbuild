
; vim:ft=lisp:

(jbuild_version 1)

; main lib
(library
  ((name logtk_solving)
   (public_name logtk.solving)
   (synopsis "solving constraints")
   (optional)
   (libraries (containers logtk msat))
   (flags (:standard -w +a-4-42-44-48-50-58-32-60@8 -color always))
   (ocamlopt_flags (:standard -O3 -bin-annot
                    -unbox-closures -unbox-closures-factor 20))
  ))



