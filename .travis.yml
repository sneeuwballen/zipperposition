language: c
install: wget https://raw.githubusercontent.com/ocaml/ocaml-ci-scripts/master/.travis-docker.sh
script: bash -ex .travis-docker.sh
services:
- docker
env:
  global:
  - PINS="zipperposition:. logtk:. libzipperposition:. zipperposition-tools:."
  - DISTRO="ubuntu-18.04"
  - PACKAGE="zipperposition" 
  matrix:
  - OCAML_VERSION="4.03"
  - OCAML_VERSION="4.05"
  - OCAML_VERSION="4.06"
  - OCAML_VERSION="4.08"
  - OCAML_VERSION="4.09"
