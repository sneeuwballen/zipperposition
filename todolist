
- a generic (bounded?) cache, based on hashtbl
- caching ordering results
- subsumption (backward and forward)
- improve efficiency of trie by replacing map by array
    (better for small maps with unfrequent updates)
