<src>: include
<containers>: include
<tests>: include
<datalog>: include
<**/*.cmx>: inline(15)
<**/*.native> or <**/*.byte>: package(num), package(dynlink), package(str), package(unix)
<tests/*.native> or <tests/*.byte>: package(dynlink), package(str), package(unix)
